SELECT DISTINCT ?ps ?lbl
WHERE {
    {
     	?ps a sh:PropertyShape  
    } UNION {
      	?ns sh:property ?ps .
      	FILTER(isBlank(?ps)) .
    }
    ?ps rdfs:comment|dcterms:description|skos:definition ?lbl
}
