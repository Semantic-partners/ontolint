SELECT DISTINCT ?ns
WHERE {
  ?ns a sh:NodeShape .
  FILTER NOT EXISTS {
    # Gather all comments
    {
      VALUES ?commentProperty { rdfs:comment dcterms:description skos:definition }
      ?ns ?commentProperty ?comment .
    }
    UNION
    {
      ?ns ?predicate ?comment .
      ?predicate a owl:AnnotationProperty ;
      rdfs:subPropertyOf* rdfs:comment .
    }
  }
}
