SELECT ?ps ?lbl
WHERE {
    {
     	?ps a sh:PropertyShape  
    } UNION {
      	?ns sh:property ?ps .
      	FILTER(isBlank(?ps)) .
    }
    ?ps rdfs:label|skos:prefLabel|skos:altLabel|skos:hiddenLabel ?lbl
}
