SELECT DISTINCT ?ps
WHERE {
    {
     	?ps a sh:PropertyShape
    } UNION {
      	?ns sh:property ?ps .
      	FILTER(isBlank(?ps)) .
    }
    FILTER NOT EXISTS { ?ps rdfs:comment|dcterms:description|skos:definition ?lbl }
}
