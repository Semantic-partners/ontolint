PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
SELECT (COALESCE(AVG(?childCount), 0) AS ?avgBranchingFactor)
WHERE {
    SELECT ?parent (COUNT(DISTINCT ?child) AS ?childCount)
    WHERE {
        ?child rdfs:subClassOf ?parent .
    }
    GROUP BY ?parent
}
